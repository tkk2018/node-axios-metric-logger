{
  "name": "axios-metric-logger",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npx tsc --build tsconfig.json",
    "clean": "npx tsc --build --clean",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "typescript": "^5.1.3"
  },
  "peerDependencies": {
    "axios": "^1.4.0",
    "axios-http-metric": "file:../axios-metric",
    "logger": "file:../logger"
  }
}
